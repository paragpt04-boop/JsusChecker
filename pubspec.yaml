name: jsus_checker
description: JsusIPTV Checker
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  share_plus: ^9.0.0
  http: ^1.2.0
  path_provider: ^2.1.0
  shared_preferences: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - android-icon/icon.png
    - assets/icon.png
