name: jsus_checker
description: JsusChecker URL Verifier
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^9.0.0
  path_provider: ^2.1.0

flutter:
  uses-material-design: true
  assets:
    - android-icon/icon.png
